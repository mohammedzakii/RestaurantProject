

<form method="Post" action="/items/SaveAdd">

    <div>
        <label for="Name">Name</label>
        <input type="text" name="Name" class="form-control" />
    </div>

    <div>
        <label for="Image">Image</label>
        <br />
        <input type="file"  name="Image"  />
    </div>

    <div>
        <label for="Description">Description</label>
        <input type="text" name="Description" class="form-control" />
    </div>

    <div>
        <label for="Price">Price</label>
        <input type="text" name="Price" class="form-control" />
    </div>

    @*<div>
        <label for="Rest_Id">Restaurant</label>
        <select name="Rest_Id" class="form-control" >

         @foreach (var dept in @ViewBag.rest)
        {
            if (dept.Id == Model.Rest_Id)
            {
                <option selected value="@dept.Id">@dept.Name</option>
            }
            else
            {
                <option  value="@dept.Id">@dept.Name</option>
            }
       }
       </select>
    </div>*@

    <div>
        <input type="submit" value="Add" class="btn btn-success"   style="margin-top:10px" />

    </div>
     

   

    
</form>
      <div>
        <a href="/items/index" class="btn btn-dark" style="margin-top:10px" > Back</a>
    </div>

